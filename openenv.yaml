name: adaptive-learning-navigator
version: 1.0

env:
  entry_point: app.py

tasks:
  - name: learning-task-1
    max_steps: 10
  - name: learning-task-2
    max_steps: 15
  - name: learning-task-3
    max_steps: 20

grader:
  type: reward
  min: 0.0
  max: 1.0